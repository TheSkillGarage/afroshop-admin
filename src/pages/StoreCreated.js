import React from "react";
import { AuthLayout, CreateStore } from "../components";

const StoreCreated = () => {
  return (
    <AuthLayout>
      <CreateStore />
    </AuthLayout>
  );
};

export default StoreCreated;
