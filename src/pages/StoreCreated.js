import React from "react";
import { AuthWrapper, CreateStore } from "../components";

const StoreCreated = () => {
  return (
    <AuthWrapper>
      <CreateStore />
    </AuthWrapper>
  );
};

export default StoreCreated;
